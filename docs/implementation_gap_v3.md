# Implementation Gap Audit for PROJECT_SPEC_FOR_CODEX_3

| Requirement | Status | Notes | Proposed implementation |
|---|---|---|---|
| 1. CLI batch processing from `input/` to `output/` | partial | CLI, scanner, worker pool now calls a real processing handler that probes, generates a recipe, writes recipe JSON, renders through per-job tmp, validates output integrity, then applies metadata full mode before final output. Dedicated per-file structured logs are still pending. | Add richer per-file logs. Step 6 commit: HEAD (this changeset). Step 7 commit link: current commit in this changeset. |
| 2. Architecture roles (Go orchestrator, FFmpeg/ffprobe/ExifTool/Python) | partial | Go orchestrator now uses ffprobe at job runtime, FFmpeg for ordered render/encode/mux, and ExifTool for clean + diversify metadata full mode; optional/required Python helper subprocess integration remains pending. | Add optional/required Python helper calls where needed. Step 6 commit: HEAD (this changeset). Step 7 commit link: current commit in this changeset. |
| 3.1.1 Startup check for dependencies is mandatory | done | `runStartupChecks` verifies `ffmpeg`, `ffprobe`, `exiftool`, `python3` before run; added focused unit tests for mandatory dependency list and fail-fast behavior on missing required tool (Step 1). | Keep mandatory checks and expand diagnostics with per-platform install hints if needed. |
| 3.1.2 Per-file recipe generation mandatory | done | Worker processing now generates a per-job recipe from the probed input and writes JSON to the planned `logs/<basename>.recipe.json` path before render. | Maintain recipe artifact compatibility as later stages add metadata. Step 6 commit: HEAD (this changeset). |
| 3.1.3 Tmp cleanup policy mandatory (success cleanup, failure preserve) | done | Worker processing renders to `tmp/job-<id>-<basename>/rendered.mp4`, removes the per-job tmp directory after successful render + metadata finalization, and preserves that tmp directory when render fails for debugging. | Implemented render success cleanup and render failure preservation with unit tests. Step 6 commit: HEAD (this changeset). Step 7 commit link: current commit in this changeset. |
| 3.1.4 Metadata full mode (clean + diversify) mandatory | done | Recipe metadata now records full mode with a clean phase and deterministic diversify tags; worker finalization runs ExifTool in two explicit phases: `-all=` clean, then tag diversification on the final output. | Implemented metadata module and recipe fields with tests for clean + diversify command order. Step 7 commit link: current commit in this changeset. |
| 3.1.5 Color transform mandatory | partial | Runtime render now includes an unconditional geometry/color filter stage before blur and pixel replacement; full external color preset loading is still pending. | Add color preset file loading while preserving the render order. Step 6 commit: HEAD (this changeset). |
| 3.1.6 Stream overlay mandatory | partial | Runtime render now applies a final overlay stage after temporal effects using configured opacity; asset-backed overlay discovery/selection remains pending. | Replace synthetic overlay source with recursive asset discovery/selection. Step 6 commit: HEAD (this changeset). |
| 3.1.7 Audio speed: base + sine mandatory | partial | Recipe has base+sine fields and render applies the base audio speed with `atempo`; runtime sine modulation remains pending. | Add time-varying audio sine modulation without breaking A/V sync validation. Step 6 commit: HEAD (this changeset). |
| 3.1.8 Video speed: base + sine mandatory | partial | Recipe has base+sine fields and render applies the base video speed in the temporal stage with `setpts`; runtime sine modulation remains pending. | Add time-varying video sine modulation and keep temporal stage after pixel replacement. Step 6 commit: HEAD (this changeset). |
| 3.1.9 Freeze micro-events (1 frame) mandatory | missing | No freeze events generated or applied. | Generate 1-frame freeze event timeline with spacing validation; apply via filter/segment assembly. |
| 3.1.10 Replace micro-events (1 frame, donor from other stream source) mandatory | missing | No replace events pipeline or donor frame sourcing. | Build donor frame extraction pool and unique one-frame replacement map per video. |
| 3.1.11 Pixel replacement neighbor-duplication mandatory | partial | Step 6: render now applies area-limited neighbor-patch pixel replacement after weak blur using the recipe-selected edge/smart area. The effect is currently implemented as an alpha-blended neighbor patch, so exact sparse per-pixel replacement semantics can still be refined. | Refine replacement mask density to match `pixel_replace_percent` exactly. Area-selection update commit: 499f574. Step 6 commit: HEAD (this changeset). |
| 4. Startup check behavior on missing dependencies | done | Mandatory dependency failures stop startup with explicit error. | Maintain fail-fast behavior with actionable install hints. Commit: this changeset. |
| 5. Input formats `.mp4/.mov/.mkv` supported; `.webm` optional | partial | Scanner supports required formats and `.webm`; acceptable but spec allows dropping webm. | Decide policy: keep `.webm` as optional support or remove to narrow scope. |
| 5. Output naming `*_processed.mp4` | done | Job output path built as `<basename>_processed.mp4`. | No change required. |
| 5. Tmp artifacts in `tmp/` | done | Worker processing creates `tmp/job-<id>-<basename>/`, routes the rendered media artifact through `rendered.mp4` inside that directory, cleans it after successful render + metadata finalization, and preserves it on render errors. | Keep future intermediate artifacts inside the existing per-job tmp directory. Step 6 commit: HEAD (this changeset). Step 7 commit link: current commit in this changeset. |
| 5. Per-file logs and recipes in `logs/` | partial | Per-file recipe JSON is now emitted to `logs/`; dedicated per-job structured log files are still pending. | Add per-job log artifacts alongside recipe JSON. Step 6 commit: HEAD (this changeset). |
| 6.1 Two-layer speed model for audio/video | partial | Step 6: render applies base speed for audio/video in the temporal phase and validates output integrity/A/V sync; sine parameters are still recipe-only at runtime. | Add sine modulation realization. Schema/randomization commit: c37011de69f3118bd7bf50d6217e126ff0945d89. Step 6 commit: HEAD (this changeset). |
| 6.1.1 Base speed sign randomization and percent range | done | Step 2: recipe randomizer chooses sign and magnitude within configured min/max percent ranges. | Implemented seeded signed-percent generation in recipe. Commit: c37011de69f3118bd7bf50d6217e126ff0945d89. |
| 6.1.2 Sine mode `lock|independent` | done | Step 2: added `av-sine-mode` and lock behavior mirrors audio sine params to video; independent uses separate draws. | Config validation + recipe generation updated. Commit: c37011de69f3118bd7bf50d6217e126ff0945d89. |
| 6.2 Freeze events random+distance constraints | done | Step 3: temporal planner now generates freeze events as one-frame points and enforces minimum distance from all other temporal events. | Implemented in recipe temporal planner; commit: f4d51f5. |
| 6.3 Replace events random+distance+unique donor constraints | done | Step 3: replace events are generated as one-frame points with cross-event distance checks and unique donor frame assignment per video. | Implemented in recipe temporal planner; commit: f4d51f5. |
| 7. Weak Gaussian blur before pixel replacement | done | Step 6: FFmpeg render graph now applies weak `gblur` immediately before the pixel replacement stage; unit/integration checks assert the order. | Preserve blur-before-pixel invariant as replacement semantics evolve. Config/recipe commit: df626a296eb2099b11d4bdefb6931a34b5d360ea. Step 6 commit: HEAD (this changeset). |
| 7.1 Area modes `edge` and `smart` | done | Step 5: smart mode now samples video frames across time intervals via FFmpeg, scores grid cells by low inter-frame motion plus homogeneous background, stores selected area/confidence in recipe, and falls back to edge mode on analyzer errors or low confidence. Python subprocess is not used for this step, so no Python package dependency check was added. | Smart analyzer and fallback behavior implemented with unit tests. Commits: df626a296eb2099b11d4bdefb6931a34b5d360ea, 499f574. |
| 8. Randomization constraints: seed reproducibility, distances, range validation | partial | Step 2: expanded min/max validation for speed/sine/counts/percents and seeded generation for temporal/pixel recipe values. Runtime enforcement still pending. | Added config validation helpers and deterministic event/parameter generation. Commit: c37011de69f3118bd7bf50d6217e126ff0945d89. |
| 9. Expanded recipe schema for temporal/pixel/metadata full mode | done | Step 2: recipe schema now contains base speed audio/video, sine params, `av_sine_mode`, freeze/replace events, min distance, pixel replacement params, and metadata full mode. | Extended `internal/recipe/recipe.go` schema and generation logic. Commit: c37011de69f3118bd7bf50d6217e126ff0945d89. |
| 10.1 Crop validation only when crop enabled | done | Crop percent validation now runs only inside the `CropEnabled` branch; tests cover disabled crop accepting zero crop ranges and enabled crop rejecting them. Step 8 commit: [`38854f8`](../commit/38854f85f9f0546ffcaed59aed69f4f2b412fa79). | Keep crop validation scoped to crop-specific flags only. |
| 10.2 Crop percent math `ratio = percent / 100.0` | done | Render crop geometry now converts percent to a ratio with `percent / 100.0`, clamps the keep-ratio and dimensions, and tests assert a 2% crop keeps 98% of a 64x64 frame. Step 8 commit: [`38854f8`](../commit/38854f85f9f0546ffcaed59aed69f4f2b412fa79). | Preserve percent-as-human-percent semantics in future crop recipe randomization. |
| 10.3 Remove `--crop-position` if only random-area used | done | Removed the unused `CropPosition` config field and `--crop-position` CLI registration; tests assert the removed flag is rejected instead of silently accepted. Step 8 commit: [`38854f8`](../commit/38854f85f9f0546ffcaed59aed69f4f2b412fa79). | Do not reintroduce positional crop modes unless recipe/runtime semantics are fully specified. |
| 10.4 Stream overlay scanning recursive | done | Added recursive stream overlay discovery with `filepath.WalkDir`, extension filtering, hidden subtree skipping, sorted results, and tests for nested overlays. Step 8 commit: [`38854f8`](../commit/38854f85f9f0546ffcaed59aed69f4f2b412fa79). | Wire this discovery into asset-backed overlay selection when that pending overlay task is implemented. |
| 10.5 ffprobe audio permissive (video-only valid) | done | ffprobe parsing now requires a video stream but allows `Audio == nil`; tests cover video-only acceptance and audio-only rejection. Render already maps audio filters only when an audio stream exists. Step 8 commit: [`38854f8`](../commit/38854f85f9f0546ffcaed59aed69f4f2b412fa79). | Keep downstream audio stages conditional on `probe.Audio != nil`. |
| 11. Recommended effect order | partial | Step 6: added render pipeline stages and FFmpeg graph in the recommended order: geometry/color → weak blur → pixel replacement → temporal speed → overlay → encode/mux. Output validation checks file integrity and A/V duration drift. Freeze/replace event realization inside the temporal stage is still pending. | Implement one-frame freeze/replace event application inside the existing temporal stage. Step 6 commit: HEAD (this changeset). |
| 12. Worker pipeline target behavior end-to-end | partial | Scan + worker pool now integrate probe → recipe JSON → tmp render → output validation → metadata clean/diversify → success cleanup. Richer staged per-file logs are still pending. | Add richer per-stage logs. Step 6 commit: HEAD (this changeset). Step 7 commit link: current commit in this changeset. |
| 13. Keep `PROJECT_SPEC_FOR_CODEX_2.md` in repo | done | V2 spec file is present alongside V3. | No action required. |

