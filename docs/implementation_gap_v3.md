# Implementation Gap Audit for PROJECT_SPEC_FOR_CODEX_3

Audit date: 2026-05-10.

This document records the current implementation status against `PROJECT_SPEC_FOR_CODEX_3.md`. Status values are:

- `done` — implemented in runtime code and covered by focused tests or e2e coverage.
- `partial` — recipe/config/test support exists, but runtime behavior is incomplete or approximate.
- `missing` — no meaningful implementation was found.

| Requirement | Status | Notes | Proposed implementation |
|---|---|---|---|
| 1. CLI batch processing from `input/` to `output/` | done | Scanner discovers supported media, jobs write `<basename>_processed.mp4`, worker e2e covers a two-file batch. | Maintain e2e coverage for audio+video and video-only inputs. |
| 2. Architecture roles (Go orchestrator, FFmpeg/ffprobe/ExifTool/Python) | done | Go orchestrates scanning, worker jobs, recipe generation, FFmpeg rendering, ffprobe probing/validation, and ExifTool metadata. Python is checked at startup; no mandatory Python subprocess is currently needed by implemented stages. | If future smart analysis moves to Python, add package checks for the exact helper dependencies. |
| 3.1.1 Startup check for `ffmpeg`/`ffprobe`/`exiftool`/`python3` | done | `runStartupChecks` iterates mandatory dependency definitions and fails startup on missing required tools; unit tests cover the required list and missing-tool diagnostics. | Keep fail-fast behavior and actionable install hints. |
| 3.1.2 Per-file recipe generation | done | Worker probing builds a per-job config, generates a recipe, writes `logs/<basename>.recipe.json`, and e2e asserts recipe artifacts. | Keep schema backward-readable while adding any future mandatory fields. |
| 3.1.3 Tmp cleanup policy | done | Worker creates `tmp/job-<id>-<basename>/`, renders into it, deletes it after successful metadata finalization, and preserves it on render failure; tests cover both success and failure. | Keep all future intermediate artifacts inside the per-job tmp directory. |
| 3.1.4 Metadata full mode clean + diversify | done | Recipe always records `clean_diversify`; metadata runner copies the rendered artifact to final output, runs ExifTool clean (`-all=`), then writes diversify tags. | Maintain command ordering and tag allowlist safety if policy support is expanded. |
| 3.1.5 Color transformation | done | Render pipeline always applies an `eq` color transform in the geometry/color stage. | Optional preset-file loading can be added later, but is not required for the v3 mandatory transform. |
| 3.1.6 Stream overlay | partial | Render pipeline always applies an overlay stage, and overlay discovery scans recursively, but runtime currently uses a generated translucent source instead of selecting an asset from `--stream-overlay-dir`. | Wire recursive overlay discovery/selection into render inputs, with deterministic seed-based selection and graceful diagnostics when no overlay asset exists. |
| 3.1.7 Audio speed: base offset + sine modulation | partial | Recipe includes base and sine parameters; runtime applies only the base speed via `atempo`. | Add real time-varying audio sine modulation while preserving A/V duration validation. |
| 3.1.8 Video speed: base offset + sine modulation | partial | Recipe includes base and sine parameters; runtime applies only the base speed via `setpts`. | Add a time-varying video speed expression or segment-based implementation for sine modulation. |
| 3.1.9 Freeze events, exactly 1 frame | partial | Recipe planner generates 1-frame event positions with minimum-distance checks, but render does not apply freeze frames. | Implement one-frame freeze application in the temporal stage and add integration tests that inspect the generated FFmpeg graph or output frames. |
| 3.1.10 Replace events, exactly 1 frame + unique donors | partial | Recipe planner generates replace positions and unique donor frame numbers, but donors are from the same probed stream timeline and render does not apply replacements. | Source donor frames from a different stream source, enforce uniqueness, and apply one-frame replacements in the temporal stage. |
| 3.1.11 Pixel replacement neighbor-duplication | partial | Render applies weak blur followed by an area-limited neighbor patch, but the implementation is alpha-blended over a region rather than sparse random per-pixel neighbor duplication on every frame at the recipe percent. | Replace the patch approximation with a deterministic sparse mask/neighbor-duplication implementation that honors `pixel_replace_percent`. |
| 4. Startup check behavior on missing dependencies | done | Missing required tools produce clear errors with install hints and stop startup before scanning/processing. | No change required. |
| 5. Input formats `.mp4`, `.mov`, `.mkv`; `.webm` optional | done | Scanner supports the required formats and also `.webm`, which v3 explicitly allows as optional support. | No change required unless project scope later narrows supported formats. |
| 5. Output naming `*_processed.mp4` | done | Job output paths are built as `<basename>_processed.mp4`. | No change required. |
| 5. Runtime dirs `tmp/` and `logs/` | done | Startup creates output/tmp/log dirs; worker writes per-file recipes and logs under `logs/` and uses per-job tmp dirs. | No change required. |
| 6.1.1 Base speed offset | done | Recipe randomly chooses signed base percentages for audio and video; render applies base factors to video (`setpts`) and audio (`atempo`). | No change required for base-only behavior. |
| 6.1.2 Sine mode `lock|independent` | partial | Config validates `lock|independent`; recipe lock mode mirrors sine parameters between audio/video and independent mode draws separately. Runtime sine modulation is still not applied. | Complete runtime sine modulation for both audio and video. |
| 6.2 Freeze random count and min distance | partial | Recipe-side count/random/min-distance generation exists; runtime realization is missing. | Apply generated freeze events during rendering. |
| 6.3 Replace random count, min distance, unique donor | partial | Recipe-side count/random/min-distance and unique donor frame generation exists; runtime realization and external stream-source donor selection are missing. | Apply generated replace events using donor frames from other stream sources. |
| 7. Weak Gaussian blur before pixel replacement | done | FFmpeg filter graph places `gblur` immediately before the pixel replacement stage; tests assert this order. | Preserve blur-before-pixel invariant. |
| 7.1 Area modes `edge` and `smart` | done | Edge mode chooses edge bands; smart mode uses the analyzer interface/FFmpeg analyzer and falls back to edge on errors or low confidence; tests cover both paths. | Tune scoring only if future visual QA requires it. |
| 8. Randomization constraints: seed, min distances, range validation | done | Recipe generation uses `cfg.Seed`; temporal planner enforces min distances where feasible; config validation checks min/max ranges, modes, counts, percentages, and metadata mode. | Consider returning a warning/error when impossible event-count/min-distance combinations produce fewer events than requested. |
| 9. Expanded recipe schema | done | Recipe includes audio/video base+sine, `av_sine_mode`, freeze/replace events, min distance, pixel replacement parameters, area selection, neighbor offset, and metadata full mode. | Add stream-overlay asset choice to the recipe when runtime asset selection is implemented. |
| 10.1 Crop validation only when crop enabled | done | Config validation gates crop percent checks behind `CropEnabled`; tests cover disabled and enabled behavior. | No change required. |
| 10.2 Crop percent math `ratio = percent / 100.0` | done | Render crop math uses `percent / 100.0`; tests assert 2% crop behavior. | No change required. |
| 10.3 Crop position flag removal | done | No `CropPosition` config field or `--crop-position` flag remains; tests assert the old flag is rejected. | No change required. |
| 10.4 Stream overlay scanning recursive | done | `overlay.Discover` uses `filepath.WalkDir`, skips hidden subtrees, filters supported video extensions, and returns sorted paths; tests cover nested overlays. | Wire discovery into render runtime to close stream-overlay partial status. |
| 10.5 ffprobe audio permissiveness | done | ffprobe parsing requires video but allows missing audio; render maps audio only when present; e2e covers video-only processing. | No change required. |
| 11. Recommended effect order | partial | Pipeline stages and tests enforce geometry/color → weak blur → pixel replacement → temporal speed → overlay → encode/mux. Freeze/replace are not yet realized inside the temporal stage. | Add freeze/replace inside the existing temporal stage without changing the recommended order. |
| 12. Worker pipeline target behavior | done | E2E covers scan → worker pool → probe → recipe JSON → render/validation → metadata full mode → per-file logs/recipes → success tmp cleanup. | Maintain e2e as the worker contract. |
| 13. Backward compatibility: keep `PROJECT_SPEC_FOR_CODEX_2.md` | done | `PROJECT_SPEC_FOR_CODEX_2.md` remains in the repository. | Preserve this file for historical reference. |
| 14. README v3 documentation | done | README documents the v3 purpose/scope, required runtime blocks, examples, migration notes, known limitations, and checks. | Keep README in sync with completed runtime work. |
